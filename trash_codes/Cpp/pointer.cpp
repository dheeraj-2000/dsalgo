#include<bits/stdc++.h>
using namespace std;
#define P(x) cout<<x<<endl;
#define take_input cin>>x;

int main(int argc, char const *argv[]) {
      /* code */
      int x;
      int* ptr = &x;
      // cin>>x;
      take_input
      P(*ptr)
      return 0;

      char* buff = new char[8];
      memset(buff, 0, 8);

      char** ptr = &buff;
      // P(**ptr)
      // P(*buff)
}
